<div id="home">
  <nav>
  <div id="nav-content">
    <h1 id="logo">Track Anything</h1>
    <%= form_for("user", :url => user_session_path) do |f| %>
      <%= f.email_field :email, placeholder: "Email" %>
      <%= f.password_field :password, placeholder: "Password" %>
      <%= f.submit 'Log In' %>
      <div class="options">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me, "Keep me logged in", class: "remember" %>
        <%= link_to "Forgot your password?", new_password_path('user'), class: "password" %>
      </div>
    <% end %>
  </div>
  </nav>
  <div id="main">
    <div id="intro">
      <div class="pitch">
        <h2>Always Be Improving</h2>
        <p>We all aspire to be the best versions of ourselves. Create goals in various categories in our lives. But they get faded away by other urgent priorities. The mission of Track Everything is to bring these to life on a consistent basis ensuring you stick to your life’s vision.</p>
        <p>Pick a category. Add questions. Choose a frequency. Reply to an email responding to those questions.</p>
      </div>
      <div class="sign-up">
        <h2>Get Started For Free</h2>
        <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
          <%= devise_error_messages! %>

          <div class="field">
            <%= f.label :email %><br />
            <%= f.email_field :email %>
          </div>

          <div class="field"><%= f.label :password %><br />
            <%= f.password_field :password %></div>

          <div class="time-zone">
            <%= f.hidden_field :time_zone %>
          </div>

          <div class="location">
            <%= f.hidden_field :location %>
          </div>

          <div><%= f.submit "Sign up" %></div>
        <% end %>

      </div>
    </div>
  </div>
</div>
